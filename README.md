# File-Organizer

A lightweight Python automation tool that organizes cluttered directories by sorting files into categorized folders based on their extensions.

Note: Need to update the root path variable so it is dynamic instead of my local directory 😒
